#include "matrix_utils.h"
#include <stdio.h>

int main() {
    int rows1, rows2, cols1, cols2;
    int matrix1[rows1][cols1], matrix2[rows2][cols2];

    void matrix_read(rows1, cols1, matrix1);
    void matrix_read(rows2, cols2, matrix2);

    void matrix_print(rows1, cols1, matrix1);
    void matrix_print(rows2, cols2, matrix2);

}